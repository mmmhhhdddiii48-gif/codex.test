# Android container (Capacitor)

This folder is reserved for the native Android project generated by Capacitor.

Generate it locally with:

```bash
npm install
npx cap add android
npx cap sync android
npx cap open android
```

After generation, commit the produced `android/` files needed by your team policy.
